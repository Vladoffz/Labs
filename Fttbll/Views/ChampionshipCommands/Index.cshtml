@model IEnumerable<Fttbll.Models.ChampionshipCommand>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Command)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Championship)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Command.CommandID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Championship.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChampionshipCommandID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChampionshipCommandID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChampionshipCommandID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
